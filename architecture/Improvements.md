Below find a list of improvement for the portfolio website.

# 1. Top Content:

The top side of the page has elements AGZ, Insight, Capabilities, Archive and Initialize Contact. When scrolling, some of those elements overlap with the rest of the page.

Proposed fix: Encompass them on an oval that matches the palette of the website so that it's always visible and accessible. Make it so that this figure does not overlap with the rest of the elements in the website.

Also, use Nanobanana to generate a logo that encompasses the theme of the website with my initials AGZ

# 2. The Analytical Dashboard:

The animations look of for a few seconds, then they look choppy. It seems there is a timer that makes them stop and start again, fix this as it looks incredibly bothersome.

Also improve the beam splitter animation, as the one line does not connect to anything while the other one has a section with troublesome colors. Also replace what appears on the observation plane. Right now we are seeing a grid like figure that when hovered on it shows a square lit up. Make a ring interference pattern appear gradually.

# 3. The Core Principle:

This section appears troublesome, as the animation appears originally then it gets bugged as the user scrolls up and down. Fix this and also make the animation react to scrolling up and down.

# 4. General Bugs.

The console shows 8 issues after some time, do not leave these errors up to chance. Thoroughly analyze them and propose and implement a fix.
