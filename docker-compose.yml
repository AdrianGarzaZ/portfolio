version: '3.8'

services:
  nextjs_v2_dev:
    image: node:24-alpine
    container_name: nextjs_v2_dev
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: npm run dev
    environment:
      - WATCHPACK_POLLING=true
